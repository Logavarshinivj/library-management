
export function ColorBox({ color }) {
  const styles = {
    height: "25px",
    width: "251px",
    background: color,
    margin: "5px 0px",
  };
  return (
    <div style={styles}>

    </div>
  );
}
